# round-robin load balancer across multiple api container replicas
events {}
http {
    upstream api_backend {
        server api:8000;
    }
    server {
        listen 8080;
        location / {
            proxy_pass http://api_backend;
        }
    }
}